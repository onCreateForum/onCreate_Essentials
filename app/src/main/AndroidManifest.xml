<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mrinalmriyo.homedemo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>

    <application
        android:allowBackup="true"
        android:screenOrientation="portrait"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Home">
        </activity>
        <activity android:name=".MemberManagement"/>
        <activity android:name=".Login"/>
        <activity android:name=".NewMemberSignUp"/>
        <activity android:name=".MarkAttendance"/>
        <activity
            android:name=".Sessions"
            android:label="Events" />
        <activity
            android:name=".AttendanceFirstPage"
            android:label="Attendance" />
        <activity
            android:name=".Feedback"
            android:label="About us" />
        <activity
            android:name=".Database"
            android:label="Members" />
        <activity
            android:name=".AttendanceSecondPage"
            android:label="Attendance" />
        <activity android:name=".EventInfo"
            android:label="Events"/>
        <activity android:name=".Launch"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>